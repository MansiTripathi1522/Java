/*
Syntex
interface{
    // deaclare constant fields
    // deaclare methods that abstract //by default.
}
*/
interface a {
    void add(); //In Interface By default this method is abstract type 
    //the class in which we will inherit our method we will define our method in that class only.

}
// we have define every method which we are declarinh in abstract cals  otherwise erroe will